# Refactor DX Interno

## Version
- v1.55

## Slice
- Slice 56: `ÑACHEC` — service layer para reasignación territorial e inicio de relevamiento

## Fecha
- 2026-03-16

## Objetivo
- Seguir adelgazando `legajos/views/nachec_operacion.py` extrayendo a service layer dos subflujos operativos con transacción propia y contratos claros.

## Alcance
- Reasignación territorial de un caso `ÑACHEC`
- Construcción de contexto de pantalla para reasignación
- Inicio de relevamiento y transición `ASIGNADO -> EN_RELEVAMIENTO`
- Construcción de contexto de SLA para la pantalla de inicio

## Archivos modificados
- `legajos/services/nachec.py`
- `legajos/views/nachec_operacion.py`
- `legajos/tests/test_nachec_operacion_services.py`

## Cambios realizados
- Se agregó en `ServicioOperacionNachec`:
  - `get_territoriales_con_carga()`
  - `build_reasignacion_context()`
  - `reasignar_territorial()`
  - `build_inicio_relevamiento_context()`
  - `iniciar_relevamiento()`
- La view ya no orquesta directamente:
  - actualización de territorial
  - reasignación de tareas pendientes
  - historial de reasignación
  - transición a `EN_RELEVAMIENTO`
  - pase de tarea `RELEVAMIENTO` a `EN_PROCESO`
  - cálculo repetido de SLA para pantalla de inicio
- Se agregaron tests de service para fijar reasignación e inicio de relevamiento.

## Impacto en DX
- Menos lógica transaccional e historial en la view
- Menos duplicación de cálculo de carga territorial y SLA
- Mejor testabilidad del flujo operativo de `ÑACHEC`
- La siguiente extracción queda más acotada al bloque realmente complejo: cierre de relevamiento, scoring y evidencias

## Riesgos residuales
- Sigue pendiente el bloque con `ContentType`, adjuntos y scoring en `finalizar_relevamiento`
- No se pudo correr la suite Django completa en este entorno por dependencias/runtime faltantes

## Validacion
- `python3 -m py_compile legajos/services/nachec.py legajos/views/nachec_operacion.py legajos/tests/test_nachec_operacion_services.py`
